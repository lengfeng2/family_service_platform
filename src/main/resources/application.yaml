server:
  port: 8080
spring:
  datasource:
    username: root
    password: abc-123
    url: jdbc:mysql://localhost:3306/family_service_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:com/szb/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    com:
      szb:
        mapper: debug